<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    android:background="@color/background_color">
    <!-- Input field for user to enter text -->
    <EditText
        android:id="@+id/inputText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="username"
        android:background="@drawable/edittext_bg"
        android:hint="@string/input_hint"
        android:inputType="text"
        android:minHeight="50dp"
        android:padding="10dp"
        android:textColor="@color/black"
        android:textSize="16sp" />
    <!-- Button to trigger sentiment analysis -->
    <Button
        android:id="@+id/btnAnalyze"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:background="@drawable/button_bg"
        android:fontFamily="sans-serif-medium"
        android:padding="12dp"
        android:text="@string/btn_analyze"
        android:textColor="@color/white"
        android:textSize="16sp" />

    <!-- ImageView to display sentiment analysis result icon -->
    <ImageView
        android:id="@+id/resultIcon"
        android:layout_width="150dp"
        android:layout_height="133dp"
        android:layout_marginTop="15dp"
        android:contentDescription="@string/result_icon_desc"
        android:visibility="visible" />  <!-- Set visible để kiểm tra -->



</LinearLayout>