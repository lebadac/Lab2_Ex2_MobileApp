<!--
    This XML defines a custom shape for a button background with a gradient effect.
    - The shape is a rectangle.
    - A diagonal gradient (45°) transitions from `button_start` to `button_end`.
    - Rounded corners with a radius of 10dp.
    - Padding applied to the left and right.
-->

<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <gradient
        android:angle="45"
        android:startColor="@color/button_start"
        android:endColor="@color/button_end"/>
    <corners android:radius="10dp"/>
    <padding android:left="10dp" android:right="10dp"/>
</shape>
